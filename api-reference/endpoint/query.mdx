---
title: "Query your BerriAI APP"
api: "GET https://api.berri.ai/query"
description: "How to query your Berri Endpoint"
---

### Query
model (optional): The name of the GPT model to use. Default is gpt-3.5-turbo.
query (required): The prompt to generate text from.
user_email (required): The email address of the user making the request.
instance_id (required): The unique ID of the instance making the request.
<ParamField query="user_email" type="string">
   The email address you are using on your berri.ai account
   Example: ishaan@berri.ai
</ParamField>
<ParamField query="instance_id" type="string">
   The instance you want to query, create_app returns an instance_id that you can query here
</ParamField>
<ParamField query="query" type="string">
   The query for your instance
   Example: "Who is ishaan?"
</ParamField>
<ParamField query="model" type="string" optional>
  There are currently 2 types of models Berri supports
  Default: Uses text-davinci-003
  1. **gpt-3.5-turbo**: Uses chatGPT turbo api
  2. **text-davinci-003**: uses the GPT-3 model 
</ParamField>

<RequestExample>
```python python
import requests

url = "https://api.berri.ai/query"

querystring = {
                "user_email": "ishaan@berri.ai",
                "instance_id": "6663d98e-f3e2-42b9-a79e-2d92199b85fa",
                "query": "who is ishaan",
                "model": "gpt-3.5-turbo"
              }

response = requests.get(url, params=querystring)

print(response.text)
```
</RequestExample>
