---
title: "Website / File Upload"
api: "POST https://sharedinfraingestserver-qic2.zeet-berri.zeet.app/berri_pipeline"
description: "This endpoint create a chatGPT app for a single website or file"
---


**3. Customer Support Bot**
Use Case: Good for doing QA on a folder of documents (.zip file containing .pdf, .docx, .txt, .pptx files)

**Spreadsheet QA**
Use Case: Good for doing QA on a .csv (e.g. which product is showing the highest MoM growth?, etc.)

**Advanced Editor**
Use Case: If you want to tinker and 


### Query

<ParamField query="user_email" type="string">
  Pass in your work / school email.
</ParamField>

<ParamField body="file" type="form-data">
  Upload the file (.docx, .pdf, .pptx)
</ParamField>


### Response

<ResponseField name="api_endpoint" type="string">
  This will return an api endpoint which you can use to query your app.
</ResponseField>

<ResponseField name="website_endpoint" type="string">
  This will return a website endpoint which you can use for quick prototyping.  
</ResponseField>

<RequestExample>

```bash Example Request
curl --location --request POST 'https://api.mintlify.com/api/user' \
--header 'Content-Type: application/json' \
--header 'Authorization: Token <token>' \
--data-raw '{
    "current_token": ""
}'
```

</RequestExample>

<ResponseExample>

```json Response
{
  "success": 1,
  "user_group": {
    "team_id": 3,
    "token": "<user_group_token_to_auth_dashboard>",
    "name": "Example 1",
    "provided_id": "example_1"
  }
}
```

</ResponseExample>
